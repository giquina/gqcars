apiVersion: v1
kind: Service
metadata:
  name: gqcars-web
  namespace: production
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: gqcars-web